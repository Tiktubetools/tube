// Forwarding module to standard build path /src/config/site-config.ts to support unified bundling and absolute resolution
import { SITE_CONFIG } from "../src/config/site-config";
export { SITE_CONFIG };
export default SITE_CONFIG;
